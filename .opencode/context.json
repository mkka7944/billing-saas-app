{
  "lastSession": "2026-06-27 — Notification fixes + MC assignment bugs",
  "currentPhase": "Pre-TestCity: JUN2026 data loaded + auth improvements deployed + bugfix batch",
  "currentStep": "6 fixes: All Staff notifications, notification panel z-index/position/error-state, MC-1 unassigned bills limit removed, Manage tab switching from hierarchy_summary to survey_units",
  "filesModified": [
    ".opencode/context.json",
    "docs/SESSION.md",
    "src/app/api/admin/notifications/route.ts",
    "src/components/notifications/notifications-bell.tsx",
    "src/lib/repositories/assignment-repository.ts",
    "src/components/assignments/uc-detail-panel.tsx"
  ],
  "keyDecisions": [
    "All Staff notification filter: .is('deleted_at', null) was wrong — staff table has no deleted_at column. Changed to .eq('is_active', true).",
    "Notification panel z-index raised to z-[1001]/z-[1002] — was hidden under sidebar (z-[1000]) and FloatingActions (z-[800]).",
    "Desktop notification panel uses dynamic position (getBoundingClientRect on button click) instead of hardcoded top-[48px].",
    "getUnassignedBills: removed &limit=1000 that broke fetchAllRows pagination. Added bill_month filter to daily_assignments subquery.",
    "getAssignmentList: UC name filter now queries survey_units directly instead of hierarchy_summary — ensures UC names are always in sync."
  ],
  "nextActions": [
    "28 June: TestCity staff training — deliver repeatedly, harden photo queue, GPS enforcement",
    "After training: build route stabilization pipeline (GPS timestamps → route_seq)",
    "Deferred: auto-advance skip delivered items, parallel photo uploads, mark-queue, pull-to-refresh, admin review tab, Drive cleanup"
  ],
  "gitCheckpoint": "b312301",
  "gitCurrentHead": "b312301 + notification fixes + MC assignment bugfixes"
}
