{
  "lastSession": "2026-06-28 — Egress triage: notification polling + redundant per-delivery queries + usage page fix",
  "currentPhase": "Egress triage — notification polling stopped on /deliver, per-delivery queries optimized, usage page no longer depends on PAT",
  "currentStep": "3 changes: stop notifications polling on /deliver, cache app_settings in mark route + remove role check, remove Management API analytics from usage route",
  "filesModified": [
    ".opencode/context.json",
    "docs/SESSION.md",
    "src/hooks/use-notifications.ts",
    "src/app/api/deliveries/mark/route.ts",
    "src/app/api/admin/usage/route.ts",
    "src/components/settings/usage-tab.tsx"
  ],
  "keyDecisions": [
    "Notification polling (90 queries/hr) was the #1 hidden egress source on /deliver — disabled entirely for that route.",
    "app_settings now cached in module scope (5min TTL) in mark route — saves 1 query per delivery.",
    "Role check removed from mark route — ownership + assignment_items table constraint is sufficient security.",
    "Usage page no longer calls Management API analytics endpoints (which need SUPABASE_ACCESS_TOKEN). Shows 'Check Supabase dashboard' link instead, same as bandwidth section.",
    "Usage route still queries DB/storage/stats via Management API database/query endpoint (works with PAT)."
  ],
  "nextActions": [
    "Push and deploy to Vercel",
    "Monitor egress dashboard — expect ~70-80% reduction from notification polling stop alone",
    "If egress still too high: batch flagged_psids and portal_images queries per HDS unit (currently 600+ individual calls per session)"
  ],
  "gitCheckpoint": "4021322",
  "gitCurrentHead": "4021322 + egress triage: notification polling, per-delivery cache, usage page fix"
}
