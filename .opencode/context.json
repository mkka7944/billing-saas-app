{
  "lastSession": "2026-06-18 (office) — live fixes + assignment docs",
  "currentPhase": "Live Monitoring — Bug Fixes",
  "currentStep": "MapFollower unmount in Live view fixed. LiveStaffList rewritten to use useDeliveryTrail (was using issued_at query, returned empty). mapZoom state added to billing store. Assignment model direction documented in MASTER.md Section 17.",
  "phaseProgress": {
    "0d": "done",
    "0e": "done",
    "B3": "done",
    "C": "partial",
    "D": "done",
    "E": "partial",
    "F": "not_started",
    "G": "partial",
    "M1": "not_started",
    "M2": "partial",
    "M3": "not_started",
    "RBAC": "not_started",
    "Z": "deferred"
  },
  "filesModified": [
    "src/stores/billing-store.ts",
    "src/components/map-view.tsx",
    "src/components/live/live-panel.tsx",
    "src/components/live/live-staff-list.tsx",
    "src/hooks/use-delivery-trail.ts",
    "src/app/api/live/delivery-trail/route.ts",
    "docs/MASTER.md",
    "docs/SESSION.md",
    ".opencode/context.json"
  ],
  "keyDecisions": [
    "Live Monitoring is a view on /map page, not a separate page. Panel uses fixed positioning with z-[9999] to stay above Leaflet elements.",
    "PKT timezone utility (src/lib/pkt.ts) created as single source of truth for all Pakistan timezone date/datetime operations. All future timestamp captures and filters should use pktToday() / pktDayRange() / pktCurrentMonth().",
    "Delivery trail API queries by delivered_at in PKT date range (not issued_at assignment date). issued_at tracks when assignment was created; delivered_at tracks actual delivery time — live monitoring cares about the latter.",
    "LiveDeliveryTrail uses dynamic(() => import(...), { ssr: false }) — same pattern as StaffMap — because react-leaflet references window during import.",
    "timestamptz column storage remains UTC — only filter computations and date-only columns changed to PKT. Zero risk to existing data.",
    "MapFollower must remain mounted during Live view — fixed by adding activeView check to mount condition.",
    "LiveStaffList rewritten to use useDeliveryTrail; no longer calls /api/staff/stats (one less API call per 5s poll).",
    "mapZoom state in billing store decouples zoom from DB settings — city=12, UC=15, house=18 defaults."
  ],
  "nextActions": [
    "Continue assignment model discussion (daily target schema, Sunday handling, target-vs-actual dashboard)",
    "Phase 2 — Staff Positions (live GPS) deferred pending assignment model discussion",
    "Phase H.4 — Section-level shimmer skeletons in HDS (~30m)",
    "Phase H.7 — Prefetch adjacent unit data (~45m)",
    "Phase F — Auto-Route Generation (3hrs)",
    "Phase E — Flag Management UI (~3hrs remaining)",
    "Phase M2 — Marker clustering + UC count badges (~1.5hrs remaining)"
  ],
  "blockers": []
}
