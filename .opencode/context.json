{
  "lastSession": "2026-06-28 — Egress emergency fixes",
  "currentPhase": "Pre-TestCity: egress emergency — delivery egress optimization",
  "currentStep": "3 egress fixes: removed photo queue invalidateQueries, staleTime 30s→5min + refetchOnWindowFocus:false, lightweight visibility sync (IDs only)",
  "filesModified": [
    ".opencode/context.json",
    "docs/SESSION.md",
    "src/hooks/use-photo-queue.ts",
    "src/hooks/use-assignments.ts",
    "src/hooks/use-assignment-realtime.ts",
    "src/app/api/assignments/route.ts",
    "src/lib/validation/schemas.ts"
  ],
  "keyDecisions": [
    "Photo queue: removed invalidateQueries(['staff-assignment']) after every queue drain — was the #1 egress source missed in June 26 fix.",
    "useStaffAssignment: staleTime 30s→5min + refetchOnWindowFocus:false + refetchOnReconnect:false — eliminates ~80% of refetches.",
    "useAssignmentRealtime: sync now fetches only item IDs via ?ids_only=true (~1KB) instead of full assignment payload (~15-333KB).",
    "New API mode: GET /api/assignments?staff_id=X&ids_only=true returns just {items: [{id}]} for lightweight sync checks."
  ],
  "nextActions": [
    "Monitor egress dashboard after deploy — expect 80-90% reduction in staff session egress",
    "Consider: batch flagged_psids and portal_images queries if egress still too high",
    "After training: build route stabilization pipeline (GPS timestamps → route_seq)"
  ],
  "gitCheckpoint": "4468b97",
  "gitCurrentHead": "4468b97 + egress emergency fixes (photo queue, staleTime, lightweight sync)"
}
